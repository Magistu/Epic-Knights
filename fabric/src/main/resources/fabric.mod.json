{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_authors}"
  ],
  "contributors": [
    "${mod_credits}"
  ],
  "contact": {
    "homepage": "${mod_home_url}",
    "sources": "${mod_sources_url}",
    "issues": "${mod_issues_url}"
  },
  "license": "${mod_license}",
  "icon": "magistuarmory.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.magistuarmory.fabric.EpicKnightsFabric"
    ]
  },
  "depends": {
    "fabric": "*",
    "minecraft": ">${minecraft_version_condition}",
    "architectury": ">=${architectury_version}",
    "cloth-config2": ">=${cloth_config_version}"
  }
}
